import React from "react";

const Header = () => {
  return (
    <header>
      <h2 className="world">Where in the world?</h2>
      <span>Dark Mode</span>
    </header>
  );
};

export default Header;
